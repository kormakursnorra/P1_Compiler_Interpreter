class LToken():
    def __init__(
            ID, ASSIGN, SEMICOL, INT, PLUS, MINUS,
            MULT, LPAREN, RPAREN, PRINT, END, ERROR):
        ID = ID
        ASSIGN = ASSIGN
        SEMICOL = SEMICOL
        INT = INT
        PLUS = PLUS
        MINUS = MINUS
        MULT = MULT
        LPAREN = LPAREN
        RPAREN = RPAREN
        PRINT = PRINT
        END = END
        ERROR = ERROR

